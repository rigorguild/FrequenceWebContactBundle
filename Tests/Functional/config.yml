framework:
    secret: test
    test:   ~
    session:
        storage_id: session.storage.filesystem
    form:            true
    csrf_protection: false
    validation:
        enabled: true
    router:
        resource: '%kernel.root_dir%/routing.yml'
    profiler: { only_exceptions: false }

swiftmailer:
    url: null://localhost
    disable_delivery: true

frequence_web_contact:
    to: 'foo@bar.baz'

twig:
    debug: true
    paths:
        '%kernel.project_dir%/../../Resources/views': 'FrequenceWebContactBundle'
